{
  "version": 2,
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ./",
  "builds": [
    {
      "src": "src/vercel.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/v1/(.*)",
      "dest": "src/vercel.ts"
    }
  ]
}